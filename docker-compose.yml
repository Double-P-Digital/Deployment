services:
  # Backend NestJS API
  backend:
    build:
      context: ./dailyguest-api
      dockerfile: backend.Dockerfile
    container_name: backend-api
    ports:
      - "3000:3000"
    env_file:
      - ./dailyguest-api/.env
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
    networks:
      - app-network

  # Frontend Next.js
  frontend:
    build:
      context: .
      dockerfile: ./Website-Adrian/frontend/frontend.Dockerfile
    container_name: frontend
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
      - PORT=3001
    depends_on:
      - backend
    restart: unless-stopped
    networks:
      - app-network

  # Dashboard React/Vite
  dashboard:
    build:
      context: ./apartment-dashboard
      dockerfile: dashboard.Dockerfile
    container_name: dashboard
    ports:
      - "5173:80"
    restart: unless-stopped
    networks:
      - app-network

volumes:
  mongodb_data:

networks:
  app-network:
    driver: bridge
